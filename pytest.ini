[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_methods = test_*

addopts = -v --tb=short

; addopts = -v --tb=short -n auto

; addopts = -v --tb=short --html=report.html

markers =
    slow: 运行较慢的测试（需要调用 API）
    xfail: 预期失败的测试（负面用例）
